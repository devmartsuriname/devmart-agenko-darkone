# Restore Point: A12.5 — Frontend/Sections Navigation (Move Hero)

**Created:** 2025-12-19  
**Phase:** A12.5  
**Scope:** Navigation restructure + 404 route behavior

---

## Decisions Applied

1. **Hero Sections MOVED** (not duplicated) — After this step, Hero Sections must NOT appear under Content anymore
2. **Media Library stays under Content** — No change to Media Library position
3. **/frontend/sections/about|contact|footer** — Must return 404 if accessed directly (no redirects)

---

## Files Modified

| File | Change |
|------|--------|
| `apps/admin/src/assets/data/menu-items.ts` | Add Frontend menu group, move Hero from Content |
| `apps/admin/src/helpers/Manu.ts` | Add `/frontend` route role requirement |
| `apps/admin/src/routes/index.tsx` | Add 404 catch-all for `/frontend/sections/*` |
| `docs/backend.md` | Mark A12.5 complete |
| `docs/architecture.md` | Mark A12.5 complete |

---

## Rollback Steps

To rollback this phase:

1. **Revert menu-items.ts:**
   - Remove the `frontend` menu group
   - Re-add `content-hero` to Content children (after FAQs, before Media Library):
   ```typescript
   {
     key: 'content-hero',
     label: 'Hero Sections',
     url: '/content/hero',
     parentKey: 'content',
   },
   ```

2. **Revert Manu.ts:**
   - Remove `/frontend` from `ROUTE_ROLE_REQUIREMENTS`

3. **Revert routes/index.tsx:**
   - Remove `/frontend/sections/*` 404 catch-all route

4. **Revert docs:**
   - Remove A12.5 entries from backend.md and architecture.md

---

## What This Phase Does NOT Include

- ❌ No new CRUD routes (Hero stays at `/content/hero`)
- ❌ No database changes
- ❌ No RLS changes
- ❌ No public app changes
- ❌ No Hero CTA field hiding
- ❌ No Darkone/Zivan UI changes
