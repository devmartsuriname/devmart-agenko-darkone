# Restore Point: A12.9 — Public Home About ↔ DB Wiring

**Created:** 2025-12-19  
**Phase:** A12.9  
**Status:** Before Implementation

---

## Scope

Wire the public Home About section to fetch data from `home_about_sections` table.

---

## Files to Modify

| File | Change |
|------|--------|
| `apps/public/src/hooks/useContent.js` | Add `useHomeAboutSections()` hook |
| `apps/public/src/components/Pages/Home.jsx` | Add `fallbackAbout`, `transformHomeAboutData`, wire `<About>` props |
| `docs/backend.md` | Add A12.9 completion entry |
| `docs/architecture.md` | Add A12.9 completion entry |

---

## Files NOT to Modify

- `apps/public/src/components/About/index.jsx` — NO UI/layout changes
- Any styling files — NO styling changes
- Any other sections (Hero, Services, Portfolio, etc.) — NOT in scope
- Database schema — NO changes
- RLS policies — NO changes

---

## Rollback Steps

1. **Revert useContent.js:**
   - Remove `useHomeAboutSections()` function

2. **Revert Home.jsx:**
   - Remove `useHomeAboutSections` import
   - Remove `fallbackAbout` constant
   - Remove `transformHomeAboutData` function
   - Restore hardcoded props on `<About>` component

3. **Revert docs:**
   - Remove A12.9 entries from `backend.md` and `architecture.md`

---

## Expected Outcome

- Home About section fetches from `home_about_sections` table
- Active sections only, ordered by `sort_order ASC`, `updated_at DESC`
- First active section used (sections[0])
- Fallback to hardcoded values when no data or fetch error
- Zivan layout 1:1 preserved (no UI changes)
